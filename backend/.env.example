# Copy to .env (gitignored). NEVER commit real secrets.
DATABASE_URL=postgres://xend:xend@localhost:5432/xend
REDIS_URL=redis://localhost:6379
RUST_LOG=info

# Phase 2: the ONLY place RPC credentials live. The device never sees these.
# SOLANA_RPC_URL=https://api.devnet.solana.com
# SOLANA_WSS_URL=wss://api.devnet.solana.com
